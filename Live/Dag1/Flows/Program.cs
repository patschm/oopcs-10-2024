namespace Flows;

internal class Program
{
    static void Main(string[] args)
    {
        do
        {

        }
        while (true);

        while (true)
        {
            //Console.WriteLine("Iteratie");
        }
        //int idx = 0;
        //for (Console.WriteLine("Init"); ;Console.WriteLine("Eind iteratie"))
        for (int idx = 0; idx < 10; idx++)
        {
            //idx += 2;
           // Console.WriteLine(idx);
            //if (idx == 10) break;
        }
        //Console.WriteLine(idx);

        int age = 10;
        // if-else wordt met  name gebruikt voor range checks
        if (age <= 10)
        {

        }
        else if (age <= 20)
        {

        }
        else
        {

        }

        // Gebruik de switch voor concrete waarde tests.
        switch (age)
        {
            case 0:
            case 1:
                Console.WriteLine("een");
                break;
            case 2:
                break;
            case 3:
                break;
            case > 10:
                break;
            default:
                break;
        }
    }
}
